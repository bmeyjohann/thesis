# Repository Guidelines

## General Guidelines

- Whenever I need to tell you to do something in a different way or am dissatisfied with your work, append concise info on what you made wrong and how to do it correctly to `AGENTS.md`. Also save a short note in codex/[appropriate_naming_for_info].md.
- Save other relevant info that should be preserved in codex/[appropriate_naming_for_info].md
- Always inform me if you save info in a file.
- Never commit the `codex/` folder; keep Codex notes local-only and out of git commits.
- When providing runnable commands for copy/paste, default to multi-line format with trailing `\` line continuations.
- Whenever project architecture changes, immediately update all architecture-related sections in `AGENTS.md` in the same work so this file stays aligned with the actual implementation and is never outdated.
- Proactively suggest automation improvements that reduce manual setup/debugging and burden for the human and increase how much Codex can run autonomously (including when escalation is needed), and include concrete next steps to implement that automation. This is about Codex being able to run for longer without requiring human intervention or oversight, so closing the look such that Codex can larger tasks autonomously by checking its own implementation and iterating on that or doing more thing by receiving the appropriate tooling.
- Subagent permission policy: subagents must not execute commands that require escalated permissions. If escalation is needed (e.g., SSH/cluster operations), subagents should return the required command/context and the main agent executes it with explicit user approval.
- STT clarification: whenever the user says or writes `1B` or `1p`, interpret it as `WANDB` every time. This mixup is caused by the user's speech-to-text tool.
- Use the dedicated `apply_patch` tool for file edits, and in general follow tool/API contracts literally: required keyword arguments, return signatures, scheduler semantics, and call-site expectations should match the active implementation exactly.
- Prefer explicit, environment-family-specific wiring over generic routing when it improves debuggability: keep maze and manipulation entrypoints, wrappers, and control paths separate and directly inspectable.
- Keep train, eval, and launch commands in lockstep: checkpoints should carry the settings needed for eval, auto-sync should be preferred over redundant manual flags, and run commands should preserve established hyperparameters while explicitly including any flags required to realize the intended mode.
- Keep reward and intervention logic deterministic, task-local, and single-source: avoid hidden decay schedules or overlapping reward paths, ground gating in meaningful environment signals, and expose thresholds/diagnostics so behavior can be tuned from data.
- Debug and review through the live codepath and existing signals first: inspect the active implementation before answering, reuse already-computed tensors/metrics before adding new forwards, log the loss and buffer diagnostics needed for comparison, and back interface changes with a targeted smoke test.
- For non-learning/debug investigations, explicitly verify end-to-end signal integrity: (1) observation/goal content, (2) action transform/clipping consistency, (3) intervention action storage format in replay/pref/demo buffers, and (4) presence/non-zero progression of the intended W&B metrics (not just summary endpoints).
- Correction noted: if a task stalls or takes longer than expected, provide frequent concrete progress updates and partial results immediately instead of waiting for a full bundle; isolate likely external/runtime issues (e.g., WSL/GLFW display) before iterating code-side fixes.
- Correction noted: when a helper CLI uses sentinel values like `0` or `-1` to mean "use the environment default", do not pass those sentinels straight into library constructors such as `gym.make`; omit the keyword entirely unless the value is actually valid for that API.
- Correction noted: keep sentinel handling consistent across sibling tools; `eval_interactive_manip.py` must mirror `vr_manip_config.py` and omit `max_episode_steps` when a loaded checkpoint carries `0`, instead of crashing during `gym.make`.
- Correction noted: when adjusting teacher/oracle behavior, keep the fix in the live oracle path and remove stale wrapper-side special cases from prior experiments so target switching stays aligned with the vanilla control flow.
- Correction noted: for clearance guards around carried objects, explicitly exclude the currently grasped target object from obstacle-height checks; otherwise the guard can chase the end effector upward indefinitely.
- Correction noted: when reporting network/stream startup state, distinguish clearly between `configured`, `connected`, and `receiving data`; do not print wording that implies a successful connection before packets are actually arriving.
- Correction noted: debug/status UIs must fit and remain readable at their default window size; avoid fixed sub-layouts that overflow cards or clip labels on the standard monitor panel.
- Correction noted: VR calibration/debugging should not rely on eval-only flows or raw OpenVR button ids; use a dedicated calibration path, keep picker labels identical to the live button visualization, and expose the real intervention/gating behavior plus raw XYZ/rotation/gripper signals together.
- Correction noted: when a teleop/controller layer targets robot-space pose errors but the environment action space is normalized, convert those errors into action units before clipping; otherwise the controller will feel artificially sluggish even when the target-tracking logic is correct.
- Correction noted: for VR teleop responsiveness, separate mapping scale from servo aggressiveness, expose the manipulation env control timestep in the dedicated calibration tool, and let target-hold combine normalized tracking-error feedback with feedforward from controller motion rather than relying on pose error alone.
- Correction noted: when wiring VR into manipulation training, reuse the saved mapping profile and persistent VR transport from calibration instead of inventing a second config path, enforce single-env human VR intervention, and synchronize live robot proprio back into the teleop mapper through the actual wrapper stack.
- Correction noted: runtime VR state and reusable human-demo datasets belong in standard user config/data directories (for example `~/.config/thesis/...`), not in `codex/`; keep `codex/` for Codex notes only.
- Correction noted: before adding parity features across environment families, inspect the live implementation first; Safety-Gym already had online HILSERL-style intervention duplication in its training loop, so new work should extend the existing path rather than re-implementing it.
- Correction noted: when a human input device is physically attached to the Windows host but training/eval runs inside WSL, do not depend on SDL/pygame device passthrough into WSL; use a host-side sender plus a WSL-side receiver so mapping/control logic stays on the consumer side.
- Correction noted: when adding launcher scripts, verify the concrete flags against the live training implementation instead of assuming future scalability; do not advertise or hardcode multi-env settings for Safety-Gym while `safetygym_utils/train.py` still runs a scalar single-env loop.
- Correction noted: when adding manip-side no-op/hold optimizations, keep them opt-in and centralized in `ManipSpaceEnv.set_control()`; do not leave the optimization enabled by default or duplicate similar zero-action logic elsewhere in the stack.
- Correction noted: when debugging human VR intervention around episode resets, inspect the interaction between the gate latch, saved VR mapping profile, and human threshold/hold-time semantics before blaming the low-level no-op path; a held gate plus `human_intervention_threshold=-1e-6` can suppress policy actions even when the human action is numerically zero.
- Correction noted: in human VR manipulation mode, `mirror_gripper_when_inactive` can generate non-`None` teleop actions while the gate is off; because the human wrapper treats any non-`None` human action as full takeover, this can accidentally override the full policy action instead of only the gripper channel.
- Correction noted: human VR partial overrides must mirror the existing agent manual-gripper semantics: when the gate is off but VR gripper mirroring is enabled, merge only channel 4 into the student action instead of routing the whole action through the full human-teacher takeover branch.
- Correction noted: when the user asks a multipart debugging/design question, do not answer only the easiest sub-question; explicitly track and respond to the remaining requested checks or changes in the same turn.
- Correction noted: when multiple UI overlays run in the same process, do not let them each open or redraw the same pygame display independently; share a single window or explicitly suppress one overlay when the other is active.
- Correction noted: for decoupled state-based viewers, keep viewer draw throttling separate from simulation stepping; if the human expects a capped control rate, add an explicit env-step FPS limit and derive obstacle shapes/orientation from live task geometry rather than drawing every object as a circle.
- Correction noted: when uncertainty/oversight signals are exploratory rather than decision-critical, do not flood Safety-Gym dashboards with every intermediate q/trigger statistic; keep only one or two aggregate uncertainty indicators plus any actively used oversight metric.
- Correction noted: when Safety-Gym uses an external pygame viewer and keyboard intervention at the same time, do not reuse `pygame.display` for the keyboard overlay; use an independent control window (for example Tk) so the viewer and keyboard-help UI do not fight over a single display surface.
- Correction noted: when the user asks for critic-to-actor ratio control, do not substitute `num_updates`; add a real `cta`/delayed-actor flag in the live CLI and updater so the schedule persists correctly across training iterations and logs actor metrics with the right denominator.
- Correction noted: do not give inferred timestamped checkpoint/model paths from queue submission times; resolve the actual created run directories on disk before handing the user eval commands, especially when queued jobs start later and receive different timestamps than their enqueue time.
- Correction noted: in dependency-light helper modules that intentionally use lazy optional imports (for example `_np()` / optional pygame), do not reference a missing global like `np` directly inside newly added UI code; stay consistent with the module’s lazy-import pattern and smoke-test the exact codepath.
- Correction noted: for manip teleoperation without stereo depth, provide the helper view in the live calibration/collection path itself; a simple XY top-down plot of effector, cubes, and target poses is more useful than asking the human to infer alignment from the MuJoCo camera alone.
- Correction noted: local OGBench training entrypoints should default `WANDB_MODE` to `online`, while cluster/SLURM runs should stay `offline`; do not force `offline` unconditionally in the core train modules and rely on launchers to override it.
- Correction noted: a HILSERL-style comparison that claims 50/50 replay-demo sampling must actually preload the intended human demo dataset first; do not propose starting with an empty demo buffer and hoping online interventions will fill it quickly enough.
- Correction noted: when the built-in train-time teacher demo prefill already satisfies the experiment requirement, prefer that explicit prefill path over inventing a separate offline dataset dependency and extra manual setup.
- Correction noted: for human manipulation runs using linked preference loss, always compare the online cumulative intervention rate against the stored replay intervention fraction before concluding anything about teacher quality. In the April 7 VR run `cube_single_task1_human_collect_norot_fixedalpha1e3_20260407_185302`, `Train/total_interventions / env_steps ≈ 0.963` while `/Buffers/pref_pairs / /Buffers/replay_size ≈ 0.034`, so the live inconsistency was not just dataset export. The manip stack now logs `Train/pref_linked_rows_per_iter`, `Train/pref_linked_fraction`, `Train/intervened_batch_fraction`, `Train/action_override_batch_fraction`, `Train/intervention_override_*`, `Train/replay_linked_intervened_fraction`, `Train/cumulative_intervention_fraction`, `Train/intervention_storage_gap_fraction`, and `Train/replay_intervention_write_mismatch_fraction` to localize this class of bug quickly.
- Correction noted: manipulation vector-env extras must prefer an explicit executed `applied_action` forwarded from the intervention wrapper instead of reconstructing it only from `teacher_intervened`, `teacher_action`, and `student_action`. The scripted teacher path happened to be self-consistent, but the April 7 human VR run exposed that reconstructing `applied_actions` and the intervention mask from wrapper `info` is too brittle in the human branch. `InterventionWrapper` now writes `info['applied_action']`, `ManipDisableRotationActionWrapper` projects that field, and `vec_env_wrapper.py` derives the effective intervention mask as `teacher_intervened OR (applied_action != student_action)` so linked-pref storage stays aligned with the actually executed action.
- Correction noted: in Safety-Gym goal tasks that can continue after a goal hit, do not derive episode success or goals solved from only the terminal-step `info`; accumulate `goal_met` across the whole episode, and expose simple reward aliases like `mean_reward` alongside the more explicit reward-channel metrics so dashboards stay comparable to manipulation runs.
- Correction noted: when adding optional `LayerNorm` to Safety-Gym actor/critic networks, construct the `nn.LayerNorm` modules on the same device as the surrounding linear layers; otherwise CUDA runs crash in the uncertainty/critic path with mixed CPU/CUDA parameters.
- Correction noted: when a manipulation no-op hold exists but a human still reports idle drift, verify the actual deadband against live teleop jitter before assuming the hold path was removed; a threshold that only catches exact zeros can behave like a regression in VR use because tiny normalized noise keeps retriggering IK updates.
- Correction noted: for manipulation no-op handling, separate arm motion from gripper motion; gripper-only commands should not force a fresh IK solve, and the arm no-op decision should be based on the arm channels rather than an aggregate all-action threshold.
- Correction noted: when a training/chat command still does not realize the intended runtime behavior, fix the live train/reset path first and prefer a checked launcher script over another long hand-written command bundle in chat.
- Correction noted: when a visualization is enabled by default in manipulation training, still expose an explicit train-side CLI flag pair to disable or re-enable it; training/debug defaults should remain controllable from the launcher instead of being hardwired in the viewer wrapper.
- Correction noted: when adding wrapper-only viewer/debug flags to OGBench vector env construction, strip them out before calling `gym.make(...)`; raw MuJoCo env constructors only accept true environment kwargs, so wrapper flags must be consumed inside `VectorizedOGBenchEnv` itself.
- Correction noted: when adding a new runtime override used by a launcher or command, wire the CLI surface in the actual train entrypoint too; patching only the downstream env builder is not enough if argument parsing still rejects the flag.
- Correction noted: when patching a reset hook or interface handoff in the live wrapper stack, verify the actual attribute names used by the local implementation before coding against them; do not assume a field name like `teleop_interface` when the wrapper stores it as `self.teleop`.
- Correction noted: for human VR intervention across episode boundaries, do not assume the sender will clear a held gate button on reset; the receiver must explicitly start the new episode with intervention latched off until the gate is released and pressed again.
- Correction noted: for VR gripper teleop, an `absolute` control mode should mean a true absolute target over the robot gripper opening range, not an anchored delta from the trigger position at gate start; otherwise release/open behavior can fail to reach the fully open state.
- Correction noted: when preserving an existing teleop workflow, keep the established trigger direction unless the user explicitly asks to change it; fix the range/normalization bug without silently flipping the control polarity.
- Correction noted: train-side manipulation passive viewers must fail open. If intervention-color tinting throws inside the vectorized viewer sync path, log it once, disable only the tinting, and keep syncing the viewer; otherwise the window can stay open but appear frozen even though training and VR intervention are still stepping underneath.
- Correction noted: keep train-side and eval-side manipulation intervention-color viewer code in lockstep. The eval `NativeViewer` path was more defensive about model-cache keys and optional viewer locks; duplicating a weaker train-side reimplementation made tinting regress only in training.
- Correction noted: when a human teleop/controller path can emit a different external action dimensionality than the inner environment (for example 4D `xyz+gripper` under `disable_rotation` wrapped around a 5D manip env), run the same action-dimension coercion in the human branch as in the agent/oracle branch before takeover or gripper-sync logic.
- Correction noted: for manipulation intervention/color-visualization smoke tests, prefer `--controller idle` over `random`; an idle student makes the divergence teacher takeover deterministic and easier to verify both from console diagnostics and in the viewer.
- Correction noted: when adding a pygame-based `rgb_array` fallback viewer to manipulation eval, do not also open the separate pygame control panel in the same process; pygame only supports one display window there, so the control panel can hide the viewer completely.
- Correction noted: when adding a lower-dimensional manipulation control mode, lock rotation in the live env/reset/target sampling path and expose the reduced action space with a dedicated outer wrapper after intervention logic; all binary-gripper and teleop helpers must then treat the last action channel as gripper instead of hardcoding index 4.
- Correction noted: for policy-map diagnostics, do not hide the action field behind separate throttle/turn heatmaps when the user needs qualitative interpretation; provide a dedicated arrow-based motion panel that directly shows the policy-induced displacement field.
- Correction noted: when a minimal ablation learns but the richer Safety-Gym trainer does not, compare the live input preprocessing first; the current Safety-Gym custom train path feeds raw state vectors straight into actor/critic, while the working minimal FastSAC path uses `EmpiricalNormalization`, and this difference is large enough to flip Q/reward learning behavior.
- Correction noted: for continuing-goal Safety-Gym tasks, `goal_success_rate` and `goals_per_episode` over 1000-step episodes can overstate policy quality; add first-goal/directness metrics (`first_goal_success_rate`, `mean_first_goal_hit_step`, within-100/200-step hit rates, reward-to-first-goal) before drawing conclusions about whether the policy actually goes to the target.
- Correction noted: when evaluating minimal Safety-Gym checkpoints outside their training loop, restore the saved observation normalizer from the checkpoint; otherwise `eval_interactive_safetygym.py` underestimates policy quality by feeding raw state vectors into a policy that was trained on normalized inputs.
- Correction noted: for pure Safety-Gym dense baselines in the custom trainer, explicitly disable custom-only preference behavior (`--pref_sample_ratio 0.0`, `--pref_rank_weight 0.0`, and usually `--pref_capacity 0`) and disable per-episode reseeding with `--no_reseed_on_episode_reset`; otherwise comparisons against the minimal trainer are confounded by hidden custom-path behavior rather than SAC itself.
- Correction noted: treat queue-only Safety-Gym anomalies as provisional until they reproduce in a direct local probe; the experiment queue can introduce stale-state noise during diagnosis, so root-cause claims should be backed by a direct non-queue run when possible.
- Correction noted: when a run is already well served by the existing experiment queue or a direct launcher, do not add extra detachment layers like `nohup` or ad hoc `tmux` wrappers; they obscure failures, complicate PATH/conda behavior, and make diagnosis harder than using the known-good queue/manual path.
- Correction noted: for dense-vs-native Safety-Gym comparisons where the goal is to inspect collisions, keep collision cost as a logged metric by default and do not add an extra `cost_penalty` shaping term unless the user explicitly asks to change the reward.
- Correction noted: when a stripped-down Safety-Gym ablation and a rebuilt human-ready path disagree on the best hyperparameters, trust the result from the rebuilt path; in the April 2 autonomous queue runs, the rebuilt minimal/custom stack with `gamma=0.99` and `step_penalty=-0.001` produced a strong `10k` eval, while the earlier `gamma=0.97` winner from a different minimal sweep regressed badly on the rebuilt stack.
- Correction noted: if the monolithic Safety-Gym trainer still underperforms after disabling preference/demo/uncertainty extras, do not keep assuming the remaining gap is cosmetic; the April 2 autonomous research showed the bad W&B metrics were real, the custom actor/critic modules themselves were fine inside the minimal loop, and the reliable fix path is to promote the rebuilt minimal human-ready trainer instead of continuing to tune the old monolithic stack.
- Correction noted: for autonomous-research Safety-Gym training, substantial runs must go through the GPU-backed experiment queue/MCP path; do not fall back to direct shell-launched training except for smoke tests or very short diagnostics, and render heavy artifacts after training rather than during the run if runtime stability is in question.
- Correction noted: when a promising early checkpoint exists but the user asks for a clean comparison, do not continue from that checkpoint; queue fresh scratch runs so the comparison is interpretable and not confounded by resumed optimizer/state history.
- Correction noted: for long-running local experiment infrastructure, do not over-optimize daemon lifecycle for zero-idle overhead. A queue that auto-starts and auto-exits based on transient MCP activity is too fragile; prefer a single long-lived worker service with explicit health/state and let the MCP act only as a thin control client.
- Correction noted: for safety-focused Safety-Gym runs, do not stop a branch just because first-goal success stays near zero in the early checkpoints; slower learning is acceptable if collision behavior is plausibly improving, so longer safety runs should be allowed to continue unless they are clearly broken or clearly dominated.
- Correction noted: in autonomous-research mode, do not end the turn with a handoff-style summary while queued experiments are still actively being monitored; stay in the polling loop, use timers/waits between inspections, and only surface interim commentary updates while the research session is still live.
- Correction noted: when multiple autonomous-research sessions share one experiment queue, treat the queue as a shared resource; set explicit owner metadata, prefer targeted per-job cancellation over global stop/pause controls, and rely on the queue's owner-aware round-robin scheduler instead of trying to jump ahead of other sessions.
- Correction noted: in vectorized manipulation demo-prefill rollouts, teacher takeover metrics must read `teacher_intervened_mask` from the batched `infos` dict rather than only `teacher_intervened`; otherwise `agent_always` teacher-prefill episodes are stored correctly via `applied_actions` but the logged prefill `teacher_fraction` can misleadingly stay at zero.
- Correction noted: manip own-method demo sampling should degrade gracefully when the demo buffer is empty or still warming up; do not require `demo_size >= b_demo` before sampling because the replay buffer samples with replacement anyway, and always log explicit `Train/demo_*` fallback metrics so “demo buffer enabled but no demos yet” is visible instead of silently behaving like replay-only training.
- Correction noted: for long-lived MCP integrations that need to stay reliable across Codex sandbox/session changes, do not depend on `uvx --from ...` dynamic bootstrap at startup; install the tool once with `uv tool install` into a stable local directory and point the MCP config directly at that fixed executable so home-dir cache/tool writes do not break transport initialization.
- Correction noted: for hosted HTTP MCP servers that authenticate via `bearer_token_env_var`, do not assume an interactive-shell export in `~/.bashrc` is visible to Codex Desktop. Put the token in a user environment source inherited by desktop-launched processes (for example `~/.profile` and `~/.config/environment.d/...`) and then restart Codex so the hosted MCP actually sees the variable.
- Correction noted: in autonomous-research mode, launching or queueing experiments is not completion; maintain an explicit objective ledger, dynamically reprioritize the todo list as evidence changes, and continue monitoring until the user's question is answered or clearly blocked.
- Correction noted: when the user asks to create an expert policy that can later serve as a teacher, do not switch the experiment over to teacher-driven HIL / expert-controller training before that expert exists; stay on the autonomous-policy objective (from scratch or checkpoint fine-tune), use only true environment/reward shaping/curriculum changes needed to make the student itself safe and goal-directed, and treat any suboptimal checkpoint as a teacher only if the user explicitly asks for that fallback.
- Correction noted: when adding a task-local Safety-Gym curriculum wrapper such as first-goal termination, keep the eval environment on the same wrapper stack as training; manually rebuilding the eval env without that wrapper invalidates collision/directness comparisons by letting post-goal behavior leak back into the metrics.
- Correction noted: when train/eval lockstep depends on pushing curriculum state through an outer wrapper like `TerminateOnGoalWrapper`, forward helper methods such as `set_total_steps()` to the inner reward wrapper; otherwise eval silently restarts curricula from zero even though the code appears to seed the current train step.
- Correction noted: `eval_interactive_safetygym.py` must restore `terminate_on_goal` and the active Safety-Gym reward-shaping flags (heading/forward/backward, clearance/cost curricula) from checkpoint args and rebuild the same wrapper stack as training; otherwise posthoc directness reviews silently fall back to full 1000-step continuing episodes and mis-rank checkpoints.
- Correction noted: when `eval_interactive_safetygym.py` loads checkpoint args, explicit CLI overrides such as `--no_terminate_on_goal`, `--no_heading_positive_only`, and `--no_scale_actor_to_env_bounds` must win over checkpoint defaults; do not let `_apply_ckpt_defaults()` overwrite an explicitly requested evaluation mode.
- Correction noted: when the user explicitly asks for overnight autonomous work plus concrete deliverables in the morning, do not emit a premature final-style reply before the actual artifacts and summary are ready; if the live polling loop must stop for product reasons, state that limitation explicitly and leave the queue in a state where the next turn can resume directly from the active branch.
- Correction noted: in unattended experiment-queue research, avoid `queue(action=\"get_job\")` because Codex Desktop can route it through an approval path; prefer `queue_status`, `list_jobs`, `read_job_log`, and direct tails of already-known local log paths instead.
- Correction noted: when the user asks for an action-space comparison, match the requested axes first; do not substitute an unrelated big-vs-small network sweep before the raw-vs-throttle comparison is established, and verify the action transform numerically before trusting the results.
- Correction noted: when queueing launcher scripts through the experiment queue, positional `args` are not shell environment assignments by default; if a launcher is configured via env vars, explicitly parse/export `KEY=VALUE` overrides or use a wrapper that does so, otherwise queued variants can silently run the default condition under the wrong job name.
- Correction noted: wrapper launchers queued through the experiment queue must not resolve sibling scripts only via `BASH_SOURCE`/`SCRIPT_DIR`; the queue copies wrappers into `experiment_queue/running`, so wrappers need a repo-cwd fallback (for example `${PWD}/scripts/...`) to find the real target script when run under the queue, and they should invoke repo launchers via `bash target.sh ...` instead of assuming the target file is executable.
- Correction noted: when a human-collected manipulation replay snapshot shows `actions != student_actions` on most rows but `teacher_intervened` stays low, do not jump to an actor-update bug first; inspect the intervention/action bookkeeping semantics and treat offline recovery results from that dataset as provisional until the stored action labels are reconciled.
- Correction noted: Windows manipulation launchers must not rely on the `fasttd3` submodule being separately installed as a standalone `fast_sac` package. The manip path imports `fasttd3.fast_sac...`, so `fasttd3/fast_sac/__init__.py` must use relative imports (`from .fast_sac ...`) and the Windows launcher should set `PYTHONPATH` to the repo root plus `fasttd3`/`ogbench` for robustness. Safety-Gym minimal launchers do not hit this packaging trap because they stay inside repo-root modules like `safetygym_utils.*`.
- Correction noted: OGBench train entrypoints must configure `MUJOCO_GL` before importing either `ogbench` itself or trainer modules that indirectly import `ogbench`. On Windows the default must be `glfw`, not `egl`, and `ogbench/ogbench/__init__.py` should only apply the EGL teardown monkey-patch when the active backend is actually `egl`.
- Correction noted: OGBench direct entrypoints on Windows must also prepend the `ogbench` and `fasttd3` submodule roots ahead of the repo root in `sys.path` (and Windows launchers should mirror that `PYTHONPATH` order). Otherwise `import ogbench` can resolve to the submodule root as a namespace package, which breaks later imports like `ogbench.wrappers` even after the `MUJOCO_GL` fix.
- Correction noted: Safety-Gym observation masking wrappers must handle the environment's 6-value step signature `(obs, reward, cost, terminated, truncated, info)` directly; generic `gym.ObservationWrapper` step forwarding can crash immediately with `ValueError: too many values to unpack`.
- Correction noted: for manip preference-loss scope ablations, do not judge `min-Q` scope only under the old lagrangian loss; in the April 5 sweep on the current single-cube/task1 no-rotation baseline, `min-Q + bradley_terry + pref_stopgrad_positive` beat both the lagrangian baseline and the all-critic Bradley-Terry variants, while hard hinge with zero margin failed early.
- Correction noted: when exposing `alpha_min`/`alpha_max` for OGBench FastSAC, enforce the bounds in the live alpha tensor path and at startup, not only nominally after the optimizer step; otherwise W&B can report `Train/alpha` above the configured max and entropy-cap ablations become invalid.
- Correction noted: with delayed actor updates (`cta_ratio > 1`), do not accumulate `alpha_value` on every critic update while dividing by only actor/alpha update counts; that inflates logged `Train/alpha` (for example `0.002` appears as `0.0035` when `num_updates=7` and actor updates happen 4 times), so alpha metrics must be accumulated only on the iterations where alpha is actually measured/updated.
- Correction noted: a faithful manipulation behavior-cloning baseline should default to demo-only expert data; if online intervention labels are aggregated during training, that is a DAgger-style ablation and must remain an explicit opt-in rather than the default BC comparison.
- Correction noted: a faithful manipulation PVP comparison should use a dedicated TD3-style trainer with paper-like two-buffer routing and takeover-start TD masking (`stop_td`), not only the approximate FastSAC-based PVP-like loss path.
- Correction noted: when a manipulation baseline uses its own trainer instead of the shared FastSAC eval path, carry over the full eval metric surface used for decisions (`avg_success_length`, `avg_final_distance`, cube-progress metrics, lethal/timeout rates) so W&B comparisons stay method-fair.
- Correction noted: in the shared OGBench updater, always initialize aggregate loss scalars like `critic_loss_total_value` before branching on algo/pref mode; HILSERL-style runs with zero pref loss can otherwise crash during metric accumulation with `UnboundLocalError` once updates start.
- Correction noted: Safety-Gym `pref_sampling_mode=linked` must match the manipulation implementation exactly: compute preference loss on the same sampled TD batch, filtered by `teacher_intervened` and paired with `student_actions`, rather than drawing a second replay-wide intervened-only preference batch.
- Correction noted: `SimpleReplayBuffer.extend()` assumes a full `n_env` batch; any algorithm branch that relabels or stages single env rows online (for example manip EIL good/bad takeover annotations) must use a dedicated single-env insert path instead of calling `extend()` with a one-row tensor dict, or vectorized runs will crash on the replay-buffer reshape.

# Knowledge Management
- The Linear team corresponding to this workspace is named Thesis, issues have the format THE-XXX
- Use this info when requested to access Linear

## Project Structure & Module Organization
- `train_*.py`: Entry points for training (RSL‑RL, PointMaze variants) plus manipulation-side comparison baselines such as HG-DAgger and behavior cloning.
- `train_fast_sac_ogbench_maze.py`: Maze-focused FastSAC entrypoint (enforces maze env family).
- `train_fast_sac_ogbench_manip.py`: Manipulation-focused FastSAC entrypoint (enforces manip env family). It now supports `--algo_variant {own,pvp,eil}` so manip-side PVP and EIL experiments run through the same train/eval/logging stack as the current own method.
- `train_behavior_cloning_ogbench_manip.py`: Manipulation-focused behavior-cloning baseline entrypoint. It runs a deterministic single-policy imitation learner on the same intervention/wrapper scaffold as the other manip baselines, and the faithful BC baseline now defaults to demo-only teacher data while leaving online teacher aggregation as an explicit opt-in.
- `train_hg_dagger_ogbench_manip.py`: Dedicated manipulation HG-DAgger entrypoint. It trains an ensemble of deterministic novice actors from aggregated teacher takeover labels, logs intervention-time ensemble doubt, and estimates a posthoc `tau` threshold without using doubt to gate interventions during training.
- `train_pvp_td3_ogbench_manip.py`: Dedicated faithful manipulation PVP entrypoint. It runs a TD3-style deterministic actor/twin-critic trainer with separate novice and intervention buffers, proxy-value propagation losses, optional reward-in-TD ablations, and paper-style takeover-start TD masking.
- `ogbench_utils/fastsac_ogbench_maze_cli.py`: Maze-specific FastSAC parser defaults and validation.
- `ogbench_utils/fastsac_ogbench_manip_cli.py`: Manip-specific FastSAC parser defaults and validation, including single-env enforcement for human VR intervention training.
- `ogbench_utils/bc_ogbench_manip_cli.py`: Manipulation-specific behavior-cloning parser defaults and validation. It mirrors the HG-DAgger entrypoint constraints on env family, state observations, and render-mode compatibility, and exposes `--bc_collect_online_teacher_data` so the faithful demo-only BC baseline can be distinguished from online aggregation ablations.
- `ogbench_utils/hgdagger_ogbench_manip_cli.py`: Manipulation-specific HG-DAgger parser defaults and validation. It exposes ensemble size, doubt/tau settings, demo-prefill collection, and intervention/data-aggregation controls for the separate imitation-learning trainer.
- `ogbench_utils/pvp_td3_ogbench_manip_cli.py`: Manipulation-specific faithful PVP parser defaults and validation. It sets TD3-style defaults (`gamma=0.99`, small learning rates, delayed policy updates, target-policy smoothing) and PVP-specific controls like balanced novice/human sampling, proxy bounds, and `stop_td` takeover masking.
- `ogbench_utils/fastsac_ogbench_maze_env.py`: Maze-specific wrapper/env assembly (`clip_actions=1.0`).
- `ogbench_utils/fastsac_ogbench_manip_env.py`: Manip-specific wrapper/env assembly (no global L2 clip), including optional train/eval human render wiring (`train_render_mode`, `eval_render_mode` with single-eval-env enforcement), optional human teleop injection into the training wrapper stack, forwarding of `--max_episode_steps` into both train and eval env construction when a positive override is provided, and a manip-only `--disable_rotation` path that passes fixed-yaw mode into the base env. Training uses the shared vector-env passive viewer path, and intervention coloring is now on by default there when `train_render_mode=human`.
- `ogbench_utils/fastsac_ogbench_maze_train.py`: Maze-specific FastSAC orchestration used by `train_fast_sac_ogbench_maze.py`. It defaults `WANDB_MODE` to `online` for local runs and `offline` under SLURM unless the environment already overrides it.
- `ogbench_utils/fastsac_ogbench_manip_train.py`: Manip-specific FastSAC orchestration used by `train_fast_sac_ogbench_manip.py`; it can attach the persistent VR receiver as the human intervention input when `--intervention_mode human --human_input_device vr` is set, loading the saved mapping profile and closing the VR source on shutdown. In `--disable_rotation` mode it builds a 4D VR mapper (`xyz + gripper`) instead of the default 5D mapper. It defaults `WANDB_MODE` to `online` for local runs and `offline` under SLURM unless the environment already overrides it, supports periodic atomic replay-buffer snapshots during manipulation training via the `--export_replay_dataset_*` flags so human-collected data is preserved even if a run is stopped early, in `--algo_variant pvp` mode it treats the main replay buffer as the novice buffer, the demo buffer as the human/intervention buffer, disables preference loss plumbing, and uses single-env buffer storage so intervention/non-intervention rows can be routed separately under vectorized collection, and in `--algo_variant eil` mode it disables the preference/alpha machinery and runs deterministic Expert Intervention Learning-style threshold/ranking supervision on the shared manip scaffold.
- `ogbench_utils/bc_ogbench_manip_train.py`: Manipulation-specific behavior-cloning trainer. It shares the manip env/intervention/prefill stack with HG-DAgger, trains a single deterministic actor by MSE on teacher-applied actions, logs BC-specific metrics, and saves checkpoints under `logs/behavior_cloning/` and `models/behavior_cloning/`. The faithful baseline now uses only teacher-prefill demo data by default; online teacher takeover aggregation is available only when `--bc_collect_online_teacher_data` is enabled.
- `ogbench_utils/hgdagger_ogbench_manip_train.py`: Manipulation-specific HG-DAgger trainer. It runs an ensemble of deterministic novice actors, aggregates teacher intervention labels online, logs ensemble disagreement (`doubt`) at takeover points, and computes a posthoc `tau` threshold from those logged doubts for downstream evaluation.
- `ogbench_utils/pvp_td3_ogbench_manip_train.py`: Manipulation-specific faithful PVP trainer. It uses a deterministic TD3 actor/twin critics, a novice replay buffer plus a human intervention buffer, balanced 50/50 sampling, proxy targets for teacher vs novice actions, and paper-style takeover-start TD masking. The reward-free paper default and a reward-in-TD ablation are both supported.
- `ogbench_utils/fastsac_ogbench_types.py`: Dataclass containers for FastSAC model/buffer/AMP/logging components.
- `fasttd3/fast_sac/fast_sac_utils.py`: Shared replay-buffer implementation. `SimpleReplayBuffer.extend()` still expects a full `n_env` batch, while `extend_single_env(env_idx, tensor_dict)` exists for algorithm branches that need to commit one annotated env row at a time without routing through the full-batch reshape.
- `ogbench_utils/fastsac_ogbench_env.py`: Shared helper utilities (tensor shaping, reward-mode trackers, legacy family helpers) used by FastSAC OGBench loop/setup modules.
- `ogbench_utils/fastsac_ogbench_setup.py`: Device/run-dir/model/buffer/updater/logging initialization helpers for FastSAC OGBench.
- `ogbench_utils/fastsac_ogbench_loop.py`: Demo prefill, evaluation metrics rollout, and main FastSAC training loop. In `algo_variant=eil`, the loop delays committing the most recent non-intervened learner transitions so the last `--eil_bad_pre_steps` rows before each takeover can be labeled as bad while older accepted learner rows are committed as good; teacher-executed intervention rows are committed immediately as good with the student proposal retained for EIL pairwise supervision, and the row-wise EIL inserts use `SimpleReplayBuffer.extend_single_env(...)` so multi-env EIL runs do not crash on replay-buffer shape assumptions.
- `scripts/run_cube_single_task1_relonly_angle_baseline_local.sh`: Shared launcher for the current single-cube/task1 no-rotation angle-baseline manip family. It accepts `KEY=VALUE` overrides so the queue can sweep `NUM_UPDATES`, `CTA_RATIO`, reward controls (`REWARD_TYPE`, `CUBE_REWARD_MODE`), preference loss/scope, entropy controls (`ALPHA_INIT`, `FIXED_ALPHA`, `ALPHA_MIN`, `ALPHA_MAX`, `ALPHA_FREEZE_STEPS`), optional student-only alpha updates, intervention duplication into the demo buffer, manip `ALGO_VARIANT={own,pvp,eil}` controls (`PVP_INCLUDE_ENV_REWARD_IN_TD=1`, `EIL_THRESHOLD`, `EIL_GOOD_MARGIN`, `EIL_BAD_MARGIN`, `EIL_PAIR_MARGIN`, `EIL_BAD_PRE_STEPS`), train-render/timing profiling overrides (`TRAIN_RENDER_MODE`, `PROFILE_TIMING`), and the periodic manipulation replay snapshot flags without copying the script.
- `scripts/run_cube_single_task1_relonly_angle_bc_local.sh`: Dedicated behavior-cloning launcher for the single-cube/task1 manipulation benchmark. It mirrors the current angle-baseline manip scaffold, defaults to a faithful demo-only BC baseline (`BC_COLLECT_ONLINE_TEACHER_DATA=0`), and exposes online teacher aggregation only as an explicit DAgger-style override.
- `scripts/run_cube_single_task1_relonly_angle_eil_local.sh`: Thin wrapper around the shared single-cube angle-baseline launcher that selects `ALGO_VARIANT=eil`, disables demo-prefill/preference plumbing, forces deterministic `alpha=0`, and exposes the EIL threshold/margin knobs for queue sweeps.
- `scripts/run_cube_single_task1_relonly_angle_hgdagger_local.sh`: Dedicated HG-DAgger launcher for the single-cube/task1 manipulation benchmark. It reuses the same observation/intervention/no-rotation scaffold but calls the separate `train_hg_dagger_ogbench_manip.py` ensemble-imitation entrypoint instead of the FastSAC trainer.
- `scripts/run_cube_single_task1_hilserl_march_repro_local.sh`: Exact-style March HILSERL reproduction launcher for single-cube/task1. It keeps the old lighter setting (`CTA_RATIO=1`, `NUM_UPDATES=1`, learned alpha, rotation enabled by default, demo prefill 20, `demo_sample_ratio=0.5`, and `store_intervened_in_demo_buffer=1`) so current comparisons can separate method effects from the newer aggressive no-rotation scaffold.
- `scripts/run_cube_single_task1_human_collect_local.sh`: Human VR intervention launcher for the current single-cube/task1 no-rotation own-method scaffold. It keeps the current preferred training settings (`CTA_RATIO=2`, `NUM_UPDATES=7`, fixed `alpha=0.001`, lagrangian/all-critic preference loss with stopgrad-positive, 20 teacher-prefill demos, replay snapshots every 1000 env steps), defaults replay snapshot exports into the repo-local ignored folder `local/ogbench_manip_datasets/` unless an explicit export path/dir override is provided, and enables `PROFILE_TIMING=1` by default so live human collection runs log per-step action/env/sample/update timing breakdowns without extra manual flags.
- `scripts/recover_offline_actor_manip_from_checkpoint.py`, `scripts/run_cube_single_task1_offline_actor_recover_local.sh`: Offline manipulation recovery utility plus queue-friendly launcher for debugging saved human-intervention runs. It loads a saved FastSAC checkpoint and exported replay snapshot, freezes the critic/normalizer, optimizes only the actor on offline states, and periodically reuses the shared manip eval path to test whether frozen-critic optimization or strong BC-style regularization can recover any autonomous behavior from the saved dataset.
- `scripts/run_cube_single_task1_relonly_angle_pvp_local.sh`: Dedicated wrapper for the matched single-cube/task1 manip PVP baseline. It reuses the shared angle-baseline launcher but forces `ALGO_VARIANT=pvp`, no teacher demo prefill, 50/50 novice-human buffer sampling, deterministic zero-alpha rollout settings, and supports the reward-in-TD ablation through `PVP_INCLUDE_ENV_REWARD_IN_TD=1`.
- `scripts/run_cube_single_task1_relonly_angle_pvp_td3_local.sh`: Dedicated faithful manipulation PVP launcher. It calls the separate TD3-style trainer with paper-like defaults, two-buffer routing, target-policy smoothing, takeover-start TD masking, and optional reward-in-TD / proxy-bound ablations.
- `eval_interactive.py`: Load a trained model and render episodes.
- `train_fast_sac_safetygym.py`, `train_fastsac_pvp_safetygym.py`, `train_fastsac_hilserl_safetygym.py`: Safety-Gymnasium state-vector FastSAC-family training entrypoints for own/PVP/HILSERL comparisons with human intervention. They now expose `--human_input_device {keyboard,gamepad}`, shared gamepad-config flags, reusable dataset preload flags, `--store_intervened_in_demo_buffer` (generic optional demo duplication on top of the existing HILSERL behavior), startup checkpoint bootstrapping (`--init_checkpoint_path` plus per-module load toggles), optional final replay/demo dataset export, configurable network size defaults (`actor_hidden_dim=256`, `critic_hidden_dim=512`), configurable critic ensemble size via `--num_critics`, LayerNorm toggles via `--use_layer_norm` / `--layer_norm_eps`, online observation normalization via `--obs_normalization` / `--no_obs_normalization` with checkpoint persistence of the normalizer state, delayed-actor/update-schedule controls via `--policy_frequency`, temperature initialization via `--alpha_init`, critic-loss aggregation via `--critic_loss_reduction`, optional actor-output scaling into env action bounds via `--scale_actor_to_env_bounds` / `--no_scale_actor_to_env_bounds`, and explicit SafetyCar control-surface selection via `--car_action_mode {raw_wheels,throttle_turn}`. They also support two decoupled viewer modes: `--render_mode pygame` with `--viewer_fps` / `--viewer_scale` runs the env in `rgb_array` mode and shows external camera frames, while `--render_mode topdown` disables MuJoCo rendering entirely and draws a cheap state-based bird's-eye view from task geometry. They also expose `--env_fps_limit` so interactive Safety-Gym runs can cap the actual env/control step rate independently of viewer redraw cadence, plus optional checkpoint-time policy-map export flags (`--viz_on_checkpoint`, `--viz_grid_resolution`, `--viz_quiver_stride`, `--viz_device`, `--viz_seed`, `--viz_first_step`, `--viz_headings_deg`, `--viz_num_rollouts`) that save SafetyGym value/policy maps and rollout overlays under each run’s `policy_maps/` log directory.
- `train_fast_sac_safetygym_minimal.py`: Rebuilt Safety-Gymnasium FastSAC entrypoint that now serves as the promoted Safety-Gym base path. It still bypasses the larger monolithic Safety-Gym updater stack and drives a single-env trainer built directly on `fasttd3/fast_sac` replay plus either the original or current actor/critic modules (`--module_impl {fastsac,custom}`), but it now exposes the full Safety-Gym method surface needed for ongoing work: plain / own / PVP / HILSERL variants (`--variant`), delayed actor updates, critic-loss reduction, alpha init/clamps, observation normalization, actor-to-env action scaling, optional first-goal episode termination via `--terminate_on_goal`, optional observation masking via `--obs_mask_mode {none,goal_only_lidar}` to zero obstacle lidar channels while preserving goal/proprioception, explicit SafetyCar action-surface selection via `--car_action_mode {raw_wheels,throttle_turn,cardinal}`, human keyboard/gamepad intervention, a rule-based lidar teacher (`--human_input_device scripted`) that merges all obstacle lidars and greedily steers through bins whose obstacle closeness stays below the goal-closeness reading, checkpoint-backed expert-policy intervention (`--human_input_device expert` with `--expert_checkpoint_path`), grounded Safety-Car shaping levers (forward reward, backward penalty, heading reward, collision-cost warmup/ramp, and keepout-based clearance penalties), preference loss controls, demo prefill/pretrain controls, dataset preload/export controls, periodic replay snapshot controls (`--export_replay_dataset_interval`, `--export_replay_dataset_path|dir|label`, `--export_replay_dataset_max_rows`), a true offline-only update mode (`--offline_only`) for fixed-dataset ablations, checkpoint warm-start controls, optional checkpoint optimizer-state saving via `--save_optimizer_state_in_checkpoints` / `--no_save_optimizer_state_in_checkpoints`, and checkpoint-time eval/viz flags (`--eval_save_episode_plots`, `--viz_on_checkpoint`, `--viz_*`).
- `eval_interactive_safetygym.py`: Safety-Gymnasium interactive eval with policy/random/human/scripted controllers and optional human intervention overlay. It now supports `--controller gamepad` plus `--human_input_device gamepad`, reusing the shared saved gamepad mapping profile, and `--controller scripted`, which runs the same merged-lidar rule-based teacher used for scripted intervention experiments. It can show an optional live reward telemetry panel (`--show_telemetry_overlay`) plotting shaped reward, dense component, and goal distance, auto-loads actor architecture flags (`actor_hidden_dim`, `use_layer_norm`, `layer_norm_eps`) from checkpoint `args.json`, restores the saved observation normalizer whenever a checkpoint includes `obs_normalizer_state_dict`, restores `scale_actor_to_env_bounds`, `car_action_mode`, `terminate_on_goal`, and the active Safety-Gym reward-shaping/curriculum flags from checkpoint args so posthoc eval uses the same control surface and wrapper stack as training. It supports both external viewer paths (`--render_mode pygame` camera frames, `--render_mode topdown` state-based bird's-eye), reports the same rollout-level reward/goal/intervention aggregates as train-time eval (`goals_solved`, dense/sparse reward returns, teacher-fraction aliases), including first-goal/directness metrics (`first_goal_success_rate`, `mean_first_goal_hit_step`, hit-within-100/200-step rates, reward-to-first-goal), opens a separate eval control window with manip-style navigation shortcuts (`Left` previous episode seed, `Right` next, `R` reset current, `Up/Down` adjust eval FPS, `Esc/Q` quit), and can now save one trajectory image per eval episode (`--save_episode_plots`, optional `--episode_plot_dir`) with the full path, numbered goal locations, and goal-hit markers.
- `safetygym_utils/`: Shared Safety-Gymnasium env/controller/wrapper/metrics/SAC helpers; reward modes are centralized here (`sparse`, `dense`, `dense_plus_sparse`, `native`, `none`) and must stay train/eval lockstep.
- `safetygym_utils/`: Shared Safety-Gymnasium env/controller/wrapper/metrics/SAC helpers; reward modes are centralized here (`sparse`, `dense`, `dense_plus_sparse`, `native`, `none`) and must stay train/eval lockstep. The rebuilt minimal path also has an opt-in `TerminateOnGoalWrapper` so stage-2 safety curricula can train on first-goal episodes instead of always inheriting the continuing-goal regime. Train logging now mirrors the manip-side SAC diagnostics with policy/update metrics such as `policy_entropy`, `log_pi_mean`, `action_l2`, `q_min_pi_mean`, `q_min_data_mean`, `q_disagreement_*`, `replay_reward_mean`, and actor/alpha update counts. Safety-Gym train entrypoints also expose `--alpha_min` / `--alpha_max`, and the updater clamps `log_alpha` after each temperature step just like the manipulation path.
- `safetygym_utils/policy_viz.py`: Safety-Gymnasium policy/value visualization helpers. It loads FastSAC checkpoints, restores the saved observation normalizer when present, respects `scale_actor_to_env_bounds` from checkpoint args for critic evaluation, quiver simulation, and rollout actions, fixes a chosen goal layout, sweeps the SafetyCar agent over XY grids at explicit headings, plots critic min-Q/disagreement plus throttle and turn maps with one-step displacement arrows, and also saves deterministic top-down rollout overlays for the same checkpoint.
- `safetygym_utils/minimal_train.py`: Rebuilt Safety-Gymnasium training loop used by `train_fast_sac_safetygym_minimal.py`. It is now the preferred base for Safety-Gym plain and human-intervention work: the loop stays close to the proven minimal SAC baseline, reuses the shared `RewardModeWrapper`, optional `TerminateOnGoalWrapper`, optional goal-only lidar observation masking, and `HumanInterventionWrapper`, supports own/PVP/HILSERL buffer semantics, optional demo prefill + demo pretraining, optional dataset preload/export, periodic replay snapshots for live intervention collection, true offline-only gradient-update runs on fixed datasets, checkpoint warm-start, checkpoint-time trajectory-sheet export, and policy-map generation on saved checkpoints, while keeping train/eval metrics aligned with the first-goal/directness rollout summaries used elsewhere. The loop now also threads grounded reward-shaping component sums (forward reward, backward penalty, heading reward) through the same episode/eval summaries used for collision and first-goal analysis. Its training logs now aggregate manipulation-style updater diagnostics over the whole log window instead of emitting only the last gradient step, including replay-vs-preference critic loss splits, total critic loss, actor SAC loss, teacher-vs-non-teacher `q_min` means, batch teacher fraction, linked-preference row counts, teacher/student preference-Q means, and preference action-delta norms. Its checkpoint writer supports suppressing optimizer-state serialization and CPU-ifies saved tensors before `torch.save()` to reduce WSL/CUDA checkpoint fragility.
- `tools/visualize_safetygym_policy_map.py`: Standalone CLI wrapper around `safetygym_utils.policy_viz` for manual generation of SafetyGym policy maps and rollout figures from saved checkpoints.
- `tools/experiment_queue_mcp/queue_engine.py`: Persistent local experiment queue engine. It manages the `experiment_queue/` runtime folders, imports queued bash scripts, executes queued jobs with a configurable queue-wide concurrency limit (default `2`), captures logs/state, supports pause/stop/cancel controls plus optional conda-env activation for queued jobs, enforces a single active daemon per queue root with on-reconcile deduping of stale duplicate job records, reports daemon health (`daemon_running`, `daemon_pid`) to MCP clients using both the worker lock and a fresh worker-heartbeat file rather than raw `/proc/<pid>` checks so stale pid text or an unrelated lock holder does not produce false-positive liveness, reconciles orphaned `running` jobs from the control plane when the worker disappears after the child exits, and stores optional `owner_label` / `owner_session_id` metadata so cross-session mutation can be guarded unless `force=true` is used. When multiple owner sessions share a queue, dequeueing is round-robin by owner identity so one session cannot monopolize all launch slots.
- `tools/experiment_queue_mcp/server.py`: FastMCP-based stdio control server for the experiment queue. The MCP now publishes only the unified `queue` tool, which dispatches by `action` across enqueue/status/log/control operations, including queue-wide concurrency control via `set_max_concurrent_jobs` and per-session queue-root switching via `set_queue_root` for workspace-local alternate queues. The server auto-starts the background queue daemon only for mutating operations and keeps the default monitoring path light and summary-oriented so unattended autonomous sessions are less likely to trigger client approval prompts.
- `tools/experiment_queue_mcp/daemon.py`: Background queue daemon. It owns the worker lock for a queue root, consumes queued jobs, stays alive until explicit shutdown or process termination, and no longer exits just because the queue goes idle.
- `.codex/config.toml`: Repo-scoped Codex project config that is safe to commit. It keeps only portable project settings plus the remote `linear` MCP. Local stdio MCP launchers such as `wandb`, `experiment_queue`, and `zotero` must live in `~/.codex/config.toml` with absolute paths because Codex Desktop does not reliably spawn MCP servers in the workspace cwd, so repo-local relative `bash scripts/...` entries can fail during startup.
- `.agents/skills/autonomous-research/`: Repo-scoped copy of the autonomous research skill so the shared long-run experimentation workflow can travel with the repository instead of only living in `~/.codex/skills`. The skill now requires an explicit objective ledger, adaptive todo reprioritization, and persistent monitoring until the research question is answered or clearly blocked.
- `xmltodict.py`: Local minimal xmltodict-compatible shim (parse/unparse subset) used to keep Safety-Gymnasium runnable in offline environments where pip installs are unavailable.
- `config/`: YAML configs (e.g., `ogbench_config.yaml`).
- `rsl_rl/`, `fasttd3/`, `ogbench/`, `IsaacLab/`: Vendor/submodules used by training.
- `scripts/`: Cluster helpers (copy logs, SSH, allocation).
- `scripts/run_experiment_queue_mcp.sh`: Wrapper that starts the local experiment queue MCP control server with repo defaults (`experiment_queue/` root, repo script roots, `fasttd3` conda env, default queue concurrency `2`). The control server auto-starts the daemon on demand.
- `scripts/run_experiment_queue_daemon.sh`: Wrapper that starts the local experiment queue daemon directly for debugging or manual recovery with the same default queue concurrency `2`. Normal Codex use should not require starting this manually because the MCP control server auto-starts it.
- `scripts/run_wandb_mcp.sh`: Repo-scoped wrapper that launches the WANDB MCP via `uvx`, resolving the binary from `UVX_BIN`, PATH, or `~/.local/bin/uvx` so the shared project config stays secret-free and more portable across machines.
- `scripts/run_zotero_mcp.sh`: Repo-scoped wrapper that launches the Zotero MCP, preferring `powershell.exe` on Windows/WSL and falling back to local `uvx` elsewhere while keeping `ZOTERO_LOCAL=true`.
- `scripts/run_safetygym_*_local.sh`: Local launchers for Safety-Gymnasium own/PVP/HILSERL runs.
- `scripts/run_safetycar_own_human_prefill20_local.sh`: Local SafetyCar own-method launcher that collects 20 human-intervened prefill episodes into replay before online learning, uses dense reward, and sets `prefill_policy=zero` so the human effectively provides the demo actions during prefill.
- `scripts/run_safetycar_own_gamepad_dense_demo20_local.sh`: Local SafetyCar own-method launcher that mirrors the human-demo SafetyCar workflow but defaults to dense reward plus Xbox/gamepad intervention, collects 20 prefill demo episodes, uses `pref_sampling_mode=linked`, defaults to `gamepad_mode=connect` so a Windows-side sender can drive the car from an Xbox controller while still allowing `GAMEPAD_MODE=local` for direct Linux gamepad use, defaults `RENDER_MODE=pygame` with configurable `VIEWER_FPS` / `VIEWER_SCALE`, accepts `RENDER_MODE=topdown` for the state-based bird's-eye viewer, and now defaults `ENV_FPS_LIMIT=30` so interactive control can be slowed back down to a fixed env/control rate even when the decoupled viewer would otherwise let the loop run flat out.
- `scripts/run_safetycar_own_dense_only_no_prefill_local.sh`: Local SafetyCar own-method dense-only baseline launcher with no demo prefill; online human intervention remains available, but optimization is driven by dense goal-distance reward only (`pref_rank_weight=0.0`).
- `scripts/run_safetycar_pretrain_dense_plus_sparse_local.sh`: Long-running SafetyCar pretraining launcher using `reward_mode=dense_plus_sparse`, no intervention, no preference loss, regular eval/checkpointing, and manual stop-on-success workflow.
- `scripts/run_safetycar_pretrain_euclidean_local.sh`: Two-stage SafetyCar stage-1 launcher for dense Euclidean-goal pretraining with no collision penalty in the reward, W&B logging enabled, deterministic `exp_name`/run naming for handoff, automatic checkpoint saving, and optional final replay-dataset export for later warm-start use.
- `scripts/run_safetycar_pretrain_densefix_local.sh`: SafetyCar stage-1 launcher for pure dense pretraining after the goal-resample dense-spike fix; it keeps collision cost separate from reward, uses deterministic run naming, and is intended for testing whether dense-only shaping now reaches the goal reliably.
- `scripts/run_safetycar_dense_only_sanity_local.sh`: Minimal SafetyCar dense-only sanity launcher for checking whether the agent can learn from the dense goal-distance reward without any human intervention or preference loss. It intentionally uses `car_wheel_command_limit=1.0`, `car_force_scale=1.0`, `learning_starts=1000`, `render_mode=none`, and still pins `--num_envs 1` because the current SafetyGym trainer remains single-env.
- `scripts/run_safetycar_native_reward_sanity_local.sh`: Minimal SafetyCar native-reward sanity launcher for checking whether the policy learns more effectively from the environment's own reward than from the custom wrapped dense reward. It mirrors the old-style dense setup (`512/1024`, `wheel/force=2.0`, `render_mode=none`), disables intervention and preference learning, and exposes `ALPHA_MIN` / `ALPHA_MAX` environment overrides for entropy-temperature clamping without forcing them in the baseline comparison.
- `scripts/run_safetycar_minimal_human_ready_local.sh`: Generic local launcher for the rebuilt minimal Safety-Gym base path. It still defaults to the high-performing dense baseline (`custom` backend, `obs_normalization`, `scale_actor_to_env_bounds`, strong car dynamics, small step penalty), but it now also forwards the Safety-Gym method knobs needed for real work: `VARIANT={plain,own,pvp,hilserl}`, optional `MAX_EPISODE_STEPS` plus `TERMINATE_ON_GOAL=1` for first-goal curricula, `CAR_ACTION_MODE={raw_wheels,throttle_turn}` for SafetyCar control-surface experiments, intervention/gamepad controls, preference/demo settings, dataset preload/export paths, periodic replay export env vars (`EXPORT_REPLAY_DATASET_INTERVAL|PATH|DIR|LABEL|MAX_ROWS`), `OFFLINE_ONLY=1` for fixed-dataset ablations, checkpoint warm-start toggles, `wandb_group`/entity, explicit actor/critic LR overrides, optional checkpoint optimizer-state suppression via `SAVE_OPTIMIZER_STATE_IN_CHECKPOINTS=0`, and checkpoint-time eval/viz flags.
- `scripts/run_safetycar_minimal_collect_intervention_dataset_local.sh`: Dedicated local collector for reusable Safety-Gym intervention datasets. It defaults to the rebuilt minimal dense baseline with live human intervention on, disables preference/demo optimization by default so collection is not coupled to a volatile own-method objective, and periodically syncs the replay buffer to disk via `EXPORT_REPLAY_DATASET_INTERVAL` so aborted runs still leave a recent `.npz`/`.json` dataset behind for later offline ablations.
- `scripts/run_safetycar_minimal_own_dataset_debug_local.sh`: Fixed-dataset own-method debug launcher for the rebuilt minimal Safety-Gym path. It requires `DEMO_DATASET_PATH=...`, now defaults `OFFLINE_ONLY=1`, loads the dataset into replay by default, and is intended for repeated critic/preference ablations on identical intervention data rather than recollecting human interventions for every trial.
- `scripts/run_safetycar_minimal_own_gamepad_dense_demo20_local.sh`: Rebuilt minimal own-method launcher for SafetyCar. It defaults to gamepad intervention, dense reward, 20 zero-policy human prefill episodes, linked preference sampling, lagrangian preference loss, and demo duplication so the rebuilt minimal path can run the same “own dense demo20” workflow without the old monolithic trainer.
- `scripts/run_safetycar_minimal_stage2_from_pretrain_local.sh`: Rebuilt minimal stage-2 launcher for SafetyCar. It bootstraps `own`, `hilserl`, or `pvp` from a saved rebuilt-minimal checkpoint, optionally loads a pretraining replay dataset, keeps intervention/gamepad controls available, and uses the rebuilt minimal trainer as the base instead of the old monolithic Safety-Gym stack.
- `scripts/run_safetycar_dense_native_alpha_floor_compare_local.sh`: Sequential WSL/local SafetyCar comparison launcher that first runs the old-style dense setup with a nonzero alpha floor and then runs the same setup with `reward_mode=native`. It defaults to `200k` steps per run, keeps eval/save/viz cadence aligned, and is intended for unattended A/B diagnosis of exploration temperature versus reward-source effects.
- `scripts/run_safetycar_dense_reward_compare_local.sh`: Sequential WSL/local SafetyCar reward-comparison launcher. It runs `dense_plus_sparse` first and then `dense` with matched single-env settings, disabled preference loss, W&B logging, and the manip-aligned network defaults so dense-reward behavior can be compared directly without hand-editing commands.
- `scripts/run_safetycar_dense_oldstyle_repro_local.sh`: SafetyCar dense-only repro launcher that restores the older successful pretraining settings (`actor_hidden_dim=512`, `critic_hidden_dim=1024`, `car_wheel_command_limit=2`, `car_force_scale=2`, `render_mode=none`), exposes optional `ALPHA_MIN` / `ALPHA_MAX` entropy-temperature clamps, and keeps `eval_interval` and `save_interval` in lockstep so checkpoint policy maps are rendered every fixed interval via `--viz_on_checkpoint`.
- `scripts/run_safetycar_minimal_plain_local.sh`: One-command launcher for the new minimal Safety-Gym FastSAC baseline. It uses delayed actor updates (`policy_frequency=2`, `num_updates=2`), critic-loss reduction `sum`, alpha floor `5e-4`, dense reward, actor rescaling to env bounds, observation normalization, and conservative `wheel/force=1.0` so a plain “should learn” baseline can be tested without the larger Safety-Gym training stack.
- `scripts/run_safetycar_minimal_overnight_matrix_local.sh`: Sequential overnight SafetyCar debugging matrix for the minimal trainer. It runs up to ten `100k`-step experiments that isolate likely causes of the learning failure: stronger vs weaker car dynamics, dense vs dense-plus-sparse vs native reward, alpha floor on/off, actor-to-env action scaling on/off, critic-loss reduction `sum` vs `mean`, delayed-actor vs every-step actor updates, and observation normalization on/off.
- `scripts/run_safetycar_minimal_gamma_step_matrix_local.sh`: Short 6-run SafetyCar matrix built on the minimal trainer for testing directness shaping. It keeps the best current dense baseline (`policy_frequency=2`, `num_updates=2`, strong `wheel/force=2.0`, no alpha floor, observation normalization, actor-to-env scaling) and varies only `gamma` (`0.99`, `0.97`, `0.95`) and a small negative step penalty (`0`, `-0.001`, `-0.002`) over a shorter `25k` default horizon.
- `scripts/run_safetycar_minimal_backend_compare_local.sh`: Short 3-run SafetyCar backend discriminator built on the minimal trainer. It keeps the clean minimal settings fixed (`policy_frequency=2`, `num_updates=2`, dense reward, `step_penalty=-0.001`, strong `wheel/force=2.0`, actor-to-env scaling, `512/1024`) and compares the original `fastsac` modules against the current custom Safety-Gym actor/critic modules under the same minimal loop, with both observation-normalized and non-normalized custom-module variants.
- `scripts/run_safetycar_custom_obsnorm_compare_local.sh`: Short 3-run SafetyCar matrix built on the main custom trainer for isolating the observation-normalization fix. It compares old-style capacity with normalization, the same setup without normalization, and the smaller current-capacity setup with normalization while keeping dense reward, `wheel/force=2.0/2.0`, and the eval/save/viz cadence fixed.
- `scripts/run_safetycar_custom_minimalized_diag_local.sh`: Trusted single-run launcher for apples-to-apples diagnosis of the main custom Safety-Gym trainer against the working minimal trainer. It exposes the minimal-style knobs through environment variables (`policy_frequency`, `updates_per_cycle`, `critic_loss_reduction`, `alpha_init`, `scale_actor_to_env_bounds`, `obs_normalization`, `gamma`, `step_penalty`, network size, and car dynamics) while keeping dense reward, eval/save cadence, policy-map export, and eval trajectory sheets on.
- `scripts/run_safetycar_stage2_from_pretrain_local.sh`: Two-stage SafetyCar stage-2 launcher that bootstraps own/HILSERL/PVP training from a saved Safety-Gym checkpoint, supports remote gamepad intervention, can optionally preload a replay dataset exported from stage 1, and auto-resolves the latest matching stage-1 checkpoint when `INIT_CHECKPOINT_PATH` is left empty.
- `scripts/safetygym_gamepad_monitor.py`: Standalone Safety-Gym gamepad monitor. It reads the local gamepad, prints live connection/axis/button state plus mapped preview action, and starts the browser-based mapping editor so device input can be debugged without stepping an environment.
- `scripts/safetygym_gamepad_config.py`: Dedicated Safety-Gym gamepad calibration tool. It runs a live Safety-Gym environment under direct gamepad control, starts the browser-based mapping editor from the same process, exposes live runtime status so single-stick mixed vs dual-stick tank mappings can be tuned against the actual robot behavior, and supports both decoupled viewer modes (`pygame` camera frames and `topdown` state-based bird's-eye) for better responsiveness under WSL.
- `scripts/safetygym_gamepad_sender.py`: Windows-host raw Safety-Gym gamepad publisher. It samples the local SDL/pygame joystick state continuously and serves newline-delimited JSON/TCP to laptop/WSL consumers so gamepad input can be used even when WSL cannot see the device directly.
- `scripts/run_safetygym_gamepad_sender.ps1`: Windows/PowerShell launcher for the raw Safety-Gym gamepad publisher. It wraps `scripts/safetygym_gamepad_sender.py` via `uv run`, so the Windows host can start the TCP sender without manual environment activation; host/port/sample rate/device index and saved mapping path can be overridden via parameters or environment variables.
- `scripts/safetygym_gamepad_sender_requirements.txt`: Minimal Windows-side requirements for the Safety-Gym gamepad publisher (`pygame`).
- `scripts/collect_safetygym_demos.py`: Safety-Gym human-demo collector that records direct keyboard/gamepad control into reusable transition datasets (`.npz` + `.json`) under `~/.config/thesis/datasets/safetygym/` unless an explicit path is provided. It supports both decoupled viewer modes (`pygame` camera frames and `topdown` state-based bird's-eye) so demo capture can avoid the slow built-in MuJoCo human renderer.
- `scripts/smoke_safetygym_all.sh`, `scripts/safetygym_matrix_local.sh`, `scripts/verify_safetygym_ckpt_eval_lockstep.sh`, `scripts/profile_safetygym_perf_matrix.sh`, `scripts/review_safetygym_checkpoints.sh`: Safety-Gymnasium automation helpers for smoke checks, matrix runs, visual checkpoint review, perf breakdown profiling, and checkpoint/eval compatibility checks.
- `scripts/smoke_fastsac_ogbench_modularization.sh`: Compile-smoke check for FastSAC OGBench maze/manip CLI+env+train modules and shared setup/loop modules.
- `scripts/run_cube_single_static_demo_dense_online.sh`: Single-cube deterministic reset test run (fixed reset seed + demo prefill + dense cube reward mode).
- `scripts/run_cube_triple_task2_relative_only_layernorm_utd_sweep.sh`: Task2 manipulation launcher that mirrors the baseline run with relative-only observations (compact proprio + relative cube features), LayerNorm enabled, and sequential UTD sweep (`num_updates=1` then `5`).
- `scripts/run_cube_triple_task2_relative_only_layernorm_manual_gate_prefill.sh`: Task2 manipulation launcher for single-env human-gated intervention training with relative-only observations, LayerNorm, W&B logging, and teacher demo prefill into the replay buffer before learning starts.
- `scripts/run_cube_triple_task2_relonly_layernorm_newteacher_utd1_60k_hilserl_local.sh`: HILSERL-style mirror of the `cube_triple_task2_relonly_layernorm_newteacher_utd1_60k` run. It first collects teacher demos via the built-in train-time prefill path into the demo buffer, then samples 50/50 replay vs demo, duplicates online intervened actions into both replay and demo buffers, and disables all preference learning so the comparison isolates demo-buffer imitation/intervention effects without needing a separate saved dataset.
- `scripts/run_cube_single_task1_relonly_layernorm_newteacher_utd1_120k_hilserl_local.sh`: One-cube, single-task HILSERL-style retry launcher. It mirrors the rel-only LayerNorm manipulation baseline on `cube-single-singletask-task1-v0`, uses `gamma=0.97`, a real `cta_ratio=2` delayed-actor schedule, collects 20 teacher demos into the demo buffer before learning, samples 50/50 replay vs demo, and duplicates later online interventions into both replay and demo buffers for a simpler comparison when the triple-cube run shows no progress.
- `scripts/run_cube_single_task1_relonly_layernorm_newteacher_utd1_120k_own_local.sh`: One-cube, single-task own-method comparison launcher paired to the HILSERL retry setup. It keeps the same env, `gamma=0.97`, `cta_ratio=2`, 20 teacher demos prefilled into the demo buffer, 50/50 replay-demo sampling, and 50% intervention-episode gating, but replaces HILSERL's demo duplication with linked preference learning and the lagrangian ranking-loss settings used by the own method.
- `scripts/run_cube_single_task1_relonly_layernorm_newteacher_utd1_120k_own_disable_rotation_local.sh`: One-cube, single-task own-method comparison launcher for fixed-yaw ablations. It mirrors the rel-only LayerNorm single-cube setup with `gamma=0.97`, `cta_ratio=2`, 20 teacher demos prefilled into the demo buffer, 50/50 replay-demo sampling, and component-threshold deviation intervention, but adds `--disable_rotation` so the exposed action space is `xyz + gripper` and sets `tolerance_yaw_value` very large to keep yaw mismatch from spuriously triggering intervention in the locked-yaw mode.
- `scripts/run_cube_triple_task5_relative_only_layernorm_utd_sweep.sh`: Task5 manipulation launcher mirroring the task2 relative-only setup (compact proprio + relative cube features), LayerNorm enabled, and sequential UTD sweep (`num_updates=1` then `5`).
- `scripts/run_cube_triple_task2_task5_relonly_layernorm_newteacher_utd1_compare.sh`: Two-run manipulation comparison launcher that runs task2 then task5 for `60k` steps each with relative-only observations, LayerNorm, fixed `num_updates=1`, and run names tagged `newteacher` for teacher-behavior comparisons.
- `scripts/run_cube_single_relonly_vr_own_demo20_local.sh`: Local single-cube manipulation launcher that mirrors the rel-only LayerNorm preference run style for `cube-single-v0`, but uses live human VR intervention plus the auto-loaded 20-demo dataset. It forces single-env human render, forwards `--max_episode_steps` into the live manip env, and prints the VR endpoint and log file before launch.
- `scripts/vr_sender.py`: Desktop-side raw VR publisher for Windows/Linux. Default mode is a persistent server (`--mode serve`) that samples demo/OpenVR controller state continuously and lets laptop eval/train processes connect to it; legacy push mode is still available. It remembers the last endpoint in the shared VR runtime state directory (`~/.config/thesis/vr/last_endpoint.json`, with legacy fallback from `codex/vr_last_endpoint.json`).
- `scripts/vr_receiver_monitor.py`: Standalone receiver/monitor for raw VR controller streams; it can either listen for a pushed stream or connect to the persistent desktop publisher using an explicit or cached endpoint, and it can show an optional pygame status window with incoming controller/button state plus live movement visualization (XY trail, Z strip, yaw, trigger, trackpad, joystick) for both controllers. Button displays use the same named aliases as the mapping picker, and the monitor shows a compact input->mapping->output summary that points to the separate web editor for changing mappings.
- `scripts/vr_mapping_web.py`: Thin launcher for the reusable local browser-based VR mapping editor service. The actual server implementation lives in `ogbench_utils/vr_mapping_web.py`; this entrypoint just starts it as a standalone tool.
- `scripts/vr_manip_config.py`: Dedicated manipulation-side VR calibration tool. It runs a live OGBench manipulation env with a null student action under the real human `InterventionWrapper`, starts the browser mapping editor from the same process, optionally shows the pygame VR monitor, and exposes live intervention/runtime status so VR button/motion/gripper mappings can be tuned outside the eval path. It updates the teleop mapper with live robot proprio so target-hold VR control and absolute gripper control can be calibrated against the actual manipulation state, exposes explicit `physics_timestep` / `control_timestep` launch settings for teleop responsiveness tuning plus a configurable runtime button for toggling gate-off gripper mirroring on and off without restarting the tool, and supports `--disable_rotation` so calibration can run in fixed-yaw 4D (`xyz + gripper`) mode.
- `scripts/collect_ogbench_manip_demos.py`: Dedicated OGBench manipulation demo collector that runs the wrapped manipulation env with direct VR teleop, can show the same pygame VR monitor plus manip XY helper view used by calibration, records transition datasets (`obs`, `action`, `next_obs`, `reward`, `done`, `truncation`, optional intervention metadata), and saves reusable task-keyed `.npz` + `.json` datasets under the shared manipulation dataset root (`~/.config/thesis/datasets/ogbench_manip/`) unless an explicit path is provided. It now defaults to `--max_episode_steps 1000` for longer human demo collection and supports `--disable_rotation` so collected actions are 4D when yaw is locked out.
- `scripts/vr_sender_requirements.txt`: Minimal desktop-side Python requirements for the persistent VR publisher (`openvr`, `pygame`).
- `sc_venv_template/`: Virtual gridworld environment setup for HPC (Isaac Sim and Isaac Lab use an apptainer container instead of a virtual environment).
- `logs/`, `models/`, `wandb/`: Outputs, artifacts, offline tracking.
- `ogbench/ogbench/manipspace/envs/manipspace_env.py`: Base relative end-effector manipulation env; zero/no-op actions now hold the previously commanded joint/gripper targets instead of re-solving IK every step, `initialize_arm()` seeds the PD actuator targets from the sampled reset pose so idle teleop does not churn nullspace targets, and no-op detection is now split by control type: arm motion (`xyz+yaw`) only skips IK when those channels are truly zero, while gripper-only commands update the gripper target without forcing a fresh arm solve. A new `disable_rotation` env mode fixes effector yaw to the downward default and forces cube/target planar yaw sampling to zero so a 4D external action space (`xyz + gripper`) can be exposed safely.
- `ogbench_utils/env_wrappers_maze.py`: maze-only wrapper stack (`FlexibleObsWrapper` + reward/intervention wrappers) and maze goal-marker coloring helper.
- `ogbench_utils/env_wrappers_maze.py`: maze-only wrapper stack and local concrete wrappers (`MazeFlexibleObsWrapper`, `MazeDetailedRewardWrapper`) plus maze goal-marker coloring helper.
- `ogbench_utils/env_wrappers_manip.py`: manipulation-only wrapper stack and local concrete wrappers (`ManipDetailedRewardWrapper`, `ManipGoalConditionedObsWrapper`, `CubeTeacherInfoAdapter`, `ManipRelativeCubeFeaturesWrapper`, `ManipRelativeOnlyObsWrapper`, `ManipDisableRotationActionWrapper`) plus cube reward-mode tracker utilities; `relative_only_obs` keeps compact proprio + optional relative features and force-disables goal concat to avoid absolute-goal leakage. `ManipDisableRotationActionWrapper` is applied after intervention wrapping so teacher/oracle logic stays on the internal 5D action space while external consumers see 4D `xyz + gripper`.
- `ogbench_utils/vr_teleop.py`: dependency-light raw VR transport and receiver utilities: local-IP discovery, JSON/TCP listen/connect/publish modes, endpoint cache helpers (`~/.config/thesis/vr/last_endpoint.json`, with legacy fallback from `codex/vr_last_endpoint.json`), persisted mapping profile helpers (`~/.config/thesis/vr/mapping_profile.json`, with legacy fallback from `codex/vr_mapping_profile.json`), sender target prompts (pygame or console), an optional status panel with controller motion/axis visualization, compact mapping summary, and an optional manip XY helper view, raw controller helpers, and a manipulation action mapper/teleop interface that converts streamed controller pose/trigger state into 4D or 5D manip actions on the receiving machine using configurable XYZ source remapping, explicit world-frame rotation-source selection, persisted gains, named-button aliases, target-hold motion control, and absolute or delta gripper control. In absolute gripper mode, trigger now commands a true absolute gripper target over the full actuator range while preserving the repo’s existing trigger direction, and `gripper_gain` scales servo aggressiveness. `target_hold` now combines normalized robot tracking-error feedback with configurable position/yaw feedforward, the mapper supports a separate runtime controller button for toggling gate-off gripper mirroring independently of the saved default, `VRTeleopInterface` hot-reloads the saved mapping profile so live web-editor changes apply without restarting the consumer, and after an episode reset it latches intervention off until the configured gate button is released and pressed again. In 4D mode, yaw is omitted and the final action channel is always treated as gripper.
- `ogbench_utils/manip_topdown.py`: reusable manipulation top-down state extractor for pygame helper views. It reconstructs workspace XY bounds, effector position, cube positions, active target block, and target mocap poses from the live wrapped env/info stream so calibration and demo collection can show a 2D alignment view without duplicating environment-specific state logic.
- `ogbench_utils/vr_mapping_web.py`: reusable VR mapping web-server module shared by the standalone browser editor and the dedicated manipulation calibration tool. It serves the mapping form, live raw VR signal preview, per-controller XYZ history plots, named button-state visualization, mapped-action preview, and optional calibration runtime status while persisting all mapping/scaling/control-mode changes, including response/feedforward gains, to the shared VR runtime state directory (`~/.config/thesis/vr/mapping_profile.json` by default). The web UI now exposes a dedicated “gripper mirror toggle button” selector, shows both the saved default and the current live gate-off gripper-mirroring state, and can preview either 5D or fixed-yaw 4D manipulation mappings depending on the caller.
- `ogbench_utils/manip_dataset_io.py`: manipulation transition dataset helpers for reusable human-demo capture. It defines the shared dataset root (`~/.config/thesis/datasets/ogbench_manip/`), task-keyed filename generation, `.npz` + sidecar `.json` save/load utilities, latest-dataset discovery by env name, metadata inspection, and row-wise replay/demo-buffer extension with observation/action shape validation.
- `safetygym_utils/gamepad.py`: shared Safety-Gym gamepad input stack. It defines the standard runtime config location (`~/.config/thesis/safetygym_gamepad/mapping_profile.json`) and endpoint cache (`~/.config/thesis/safetygym_gamepad/last_endpoint.json`), gamepad mapping dataclass/options, live SDL/pygame joystick reader, raw JSON/TCP sender/receiver transport for host-to-WSL forwarding, same-label axis/button naming for config+monitor, hot-reloaded saved mapping support, and direct action preview/application for both car differential-wheel and planar Safety-Gym control schemes.
- `safetygym_utils/controllers.py`: shared Safety-Gym intervention-controller builders. In addition to keyboard/gamepad input, it now provides a checkpoint-backed `ExpertPolicyController` so the rebuilt minimal trainer can emulate an expert “human” teacher from a saved policy checkpoint without depending on the old monolithic trainer’s agent-teacher path, plus a `ScriptedLidarTeacherController` that merges all non-goal lidar baskets into one obstacle field and greedily rotates/drives toward the closest goal-consistent clear basket. Keyboard control still uses a dedicated Tk window when an external Safety-Gym viewer (`render_mode=pygame` or `topdown`) is active, so the control help window stays independent from the external pygame render window while preserving the existing pygame-only keyboard overlay fallback.
- `safetygym_utils/gamepad_web.py`: reusable local browser-based Safety-Gym gamepad mapping editor. It serves the saved mapping form, live device state (all named axes and buttons), mapped-action preview, and optional runtime card used by the live calibration tool.
- `safetygym_utils/rendering.py`: Safety-Gym external viewer helpers. `resolve_env_render_mode()` maps user-facing `render_mode=pygame` to underlying `rgb_array` and `render_mode=topdown` to `none`. `PygameRGBArrayViewer` throttles external camera-frame display, while `extract_safety_topdown_state()` plus `PygameTopDownViewer` rebuild a cheap bird's-eye view directly from task geometry (agent pose, footprint, forward axis, goal, hazards, obstacles) so training, eval, calibration, and demo collection can show a live window without any MuJoCo rendering cost inside `env.step`. The top-down view now derives obstacle geometry from `world_config_dict` and live body poses so box obstacles keep their footprint/orientation instead of being rendered as generic circles.
- `safetygym_utils/env.py`: Safety-Gym env assembly and action-surface adapters. `CarActuationWrapper` exposes the raw wheel-space range used by the strong baseline. `CarThrottleTurnActionWrapper` now preserves the full wheel-command authority when mapping `[throttle, turn]` into differential-wheel commands, and `CarCardinalActionWrapper` exposes the same 2D input interface but snaps execution to four primitives (`forward`, `backward`, `turn-left`, `turn-right`) plus an optional deadzone stop. `resolve_control_scheme()` treats both `throttle_turn` and `cardinal` as planar-velocity-style control surfaces for human input tools.
- `safetygym_utils/metrics.py`: shared Safety-Gym rollout-window helpers. `EpisodeWindow.summary()` produces mean metrics over recent completed episodes, and `augment_rollout_summary()` now adds concise dashboard aliases/counts (`goal_success_rate`, `goals_solved`, `goals_reached`, `goals_attempted`, `teacher_fraction_steps`, `mean_reward`, `mean_dense_reward`, `mean_sparse_reward`, `mean_env_reward`) so train/eval dashboards expose comparable success/intervention/reward summaries without the older redundant reward-return aliases.
- `safetygym_utils/dataset_io.py`: Safety-Gym transition dataset helpers for reusable human-demo capture and reload. It defines the shared dataset root (`~/.config/thesis/datasets/safetygym/`), task-keyed filename generation, `.npz` + sidecar `.json` save/load utilities, latest-dataset discovery by env name, replay-buffer extension with optional intervened/non-intervened filtering, and export of live replay/demo buffers back into reusable transition datasets.
- `ogbench_utils/env_wrappers_common.py`: shared wrapper helpers (`FixedResetSeedWrapper`, `TeleopRobotStateSyncWrapper`, environment-family inference, intervention wrapper construction).
- `ogbench_utils/intervention_wrappers.py`: local copy of intervention wrapper implementation used by `env_wrappers_common.py` (no dependency on `ogbench.wrappers` for intervention logic), including manual-gate control-panel diagnostics (`Q_min`, `Q_dis`), `agent_mode='always'` support, component-wise intervention thresholds (`xyz`, `yaw`, `gripper`) with optional adaptive tightening near pick/place targets, `manual_gripper` semantics where manual gate gives full teacher takeover while off-gate steps still mirror the teacher gripper sign (`open` and `close`) when the student disagrees, and a human-mode teleop reset on episode reset so VR target-hold anchors do not persist across episodes.
- `eval_interactive_manip.py`: standalone manipulation evaluator now supports `--controller vr`; by default it connects to a persistent desktop VR publisher using an explicit or cached endpoint, with optional fallback listen mode. It can show a pygame status panel with connection info and live controller state, logs explicit VR link/data state transitions (`configured`, `connected`, `present/none`), and now loads the persisted VR mapping profile (`--vr_mapping_path`, default `~/.config/thesis/vr/mapping_profile.json`) so the monitor-edited hand/button/axis/gain settings are reused automatically by eval. It also supports `--disable_rotation`, which locks the environment to fixed yaw and exposes a 4D `xyz + gripper` action space to keyboard/VR/policy control. With `--visualize_intervention_colors`, the native MuJoCo viewer now tints the UR5e arm and gripper materials live from `teacher_reason` diagnostics: full takeover colors arm+gripper red, while gripper-only overrides color just the gripper amber. When the native passive viewer is visually stale under WSL/GLFW, `--viewer_backend rgb_array` now provides a pygame window fed by `env.render()` so motion can still be inspected.
- `ogbench/ogbench/manipspace/oracles/markov/cube_markov.py`: cube markov oracle keeps the vanilla phase sequence, but now gates XY travel in phases 1, 5, and 9 behind an all-cubes clearance check so the effector first lifts to `max(block_z) + 0.05` before lateral motion; `ogbench_utils/intervention_wrappers.py` uses the normal oracle reset-on-done-or-target-switch flow for `cube_markov`.
- `ogbench_utils/cli.py`, `ogbench_utils/fastsac_ogbench_loop.py`: demo prefill supports a dedicated env count via `--demo_prefill_num_envs`; prefill rendering is forced off so fast teacher collection can run in parallel before single-env human-visible training starts, multi-env prefill is row-wise copied into single-env replay buffers safely, prefill progress is logged to W&B under `DemoPrefill/*`, console heartbeats are emitted during prefill, and episode-target prefill now stages per-env trajectories so `--demo_prefill_episodes N` means exactly `N` committed completed demo episodes (no overshoot). `ogbench_utils/cli.py` also now exposes the persistent VR human-input flags used by manipulation training (`--human_input_device vr`, `--vr_*`, and configurable human intervention threshold/hold settings), the online HILSERL-style duplication flag (`--store_intervened_in_demo_buffer`), reusable manipulation dataset preload controls (`--demo_dataset_path`, `--demo_dataset_dir`, `--demo_dataset_auto_load`, `--demo_dataset_target`, `--demo_dataset_max_rows`), explicit entropy-temperature controls (`--alpha_init`, `--fixed_alpha`, `--alpha_update_student_only`), a preference-critic scope switch (`--pref_critic_scope {all,min}`), and EIL-specific threshold/window controls (`--eil_threshold`, `--eil_good_margin`, `--eil_bad_margin`, `--eil_pair_margin`, `--eil_bad_pre_steps`) so manip runs can either learn alpha normally, freeze it at a constant nonzero value, run with exact `alpha=0`, ablate alpha updates to use only non-intervened student rows from replay/demo batches, compare per-head preference enforcement against min-Q-only preference enforcement, or run deterministic Expert Intervention Learning-style supervision from the same intervention traces. When `--store_intervened_in_demo_buffer` is enabled, the live training loop copies intervened rows into the single-env demo buffer in addition to the main replay buffer, linked-preference own-method runs can oversample those duplicated rows while still computing preference loss from them, and the loop logs a cumulative `Train/total_interventions` W&B metric.
- `fasttd3/fast_sac/fast_sac_utils.py`, `ogbench_utils/fastsac_ogbench_loop.py`: in `pref_sampling_mode=linked`, W&B buffer logging now reports pref-pair count from replay rows marked `teacher_intervened` rather than the standalone `PreferencePairBuffer`, so `/Buffers/pref_pairs` reflects collectable linked preference pairs stored in replay.
- `safetygym_utils/sac.py`: Safety-Gymnasium SAC now uses a local configurable actor/critic implementation that preserves old 2-critic checkpoint key names (`qnet1`/`qnet2`) but adds `--num_critics`, `--use_layer_norm`, and `--layer_norm_eps`. The update path now computes target/actor losses over the critic ensemble min and still supports manipulation-style preference optimization controls (`margin`, `bradley_terry`, `lagrangian`, `pref_stopgrad_positive`, dual lambda/EMA/violation settings) with associated metrics.
- `safetygym_utils/train.py`, `safetygym_utils/dataset_io.py`: Safety-Gymnasium console output now includes concise human-readable summaries for prefill, pretrain, train, and eval in addition to the existing JSON/W&B logging; eval summaries explicitly surface cost/collision metrics while reward remains controlled solely by `RewardModeWrapper`. The training path now supports keyboard or gamepad human input via a shared controller builder, including remote gamepad reception from a Windows host sender (`--gamepad_mode connect`), auto-loads reusable offline datasets by env name, can split loaded datasets according to the active variant (`own`, `hilserl`, `pvp`), preserves the existing online HILSERL behavior where live intervened transitions populate both replay and demo buffers, lets `variant=own` also oversample the demo buffer when `--store_intervened_in_demo_buffer` and `--demo_sample_ratio > 0` are enabled, exports a cumulative `train/total_interventions` W&B metric, can initialize actor/critic/target/alpha from a previous Safety-Gym checkpoint at startup, can export final replay/demo buffers as reusable datasets at shutdown, supports manipulation-style linked preference sampling in the own-method path via `--pref_sampling_mode linked`, storing `student_actions` and `teacher_intervened` in replay/datasets so preference batches can be derived directly from replay rows, and can drive either external viewer mode (`pygame` frame-viewer or `topdown` bird's-eye) while keeping the env itself on `rgb_array` or `none`. Train/eval logging now uses a reduced reward dashboard surface: episode-level `mean_reward`, `mean_dense_reward`, `mean_sparse_reward`, goal counts/rates, plus live-window step means (`mean_step_reward`, `mean_step_dense_reward`, `mean_step_sparse_reward`) instead of the older duplicated `live_reward_*` stat families, while the uncertainty dashboard has been cut back to a minimal aggregate (`train/uncertainty_all_*`, `train/uncertainty_intervention_*`) plus optional oversight fraction/EMA when that mode is enabled.
- `safetygym_utils/wrappers.py`, `train_fast_sac_safetygym.py`, `eval_interactive_safetygym.py`: Safety-Gymnasium reward modes now include `dense_plus_sparse` plus the alias `dual`, which adds the sparse goal-completion bonus on top of dense goal-distance progress while still keeping collision cost separate from reward. The wrapper also supports `native`, which passes through the environment's own reward unchanged for A/B comparisons against the custom dense shaping. In the rebuilt minimal path, `RewardModeWrapper` additionally supports grounded Safety-Car shaping terms based on live geometry and velocity: forward reward, backward penalty, heading-to-goal reward, cost-penalty warmup/ramp scheduling, and a keepout-based clearance penalty against constrained obstacles. Both cost and clearance penalties now support explicit warmup/ramp curricula, and the reward wrapper logs dense/sparse/step-penalty/cost-penalty/clearance/forward/backward/heading components plus goal-distance deltas and minimum constrained clearance into `info` for train/eval telemetry. It also exposes `set_total_steps()` so eval can mirror train-time curriculum schedules instead of restarting them from zero.
- `scripts/postprocess_safetycar_minimal_checkpoint_local.sh`: lightweight post-training artifact script for rebuilt Safety-Gym runs. Given a minimal checkpoint path, it renders the policy/Q/action map, rollout overlay, and a headless eval trajectory contact sheet into the corresponding `logs/safetygym_minimal/...` run directory so overnight queue jobs can leave inspection-ready artifacts without running heavy visualization during training.
- `scripts/run_safetycar_minimal_own_gamepad_from_scratch_local.sh`, `scripts/run_safetycar_minimal_own_expert_teacher_local.sh`: thin rebuilt-Safety-Gym launchers for the next intervention stage. The first starts an own-method run from scratch with a real gamepad teacher; the second swaps in a checkpoint-backed expert controller as the “human” source so the rebuilt intervention path can be smoke-tested without a live operator.
- `scripts/run_safetycar_minimal_own_gamepad_dense_best_local.sh`: recommended rebuilt-Safety-Gym dense own-method launcher for live human teaching on the promoted minimal stack. It keeps the empirically best base learner settings (`custom` modules, big `512/1024` nets, `gamma=0.99`, `step_penalty=-0.001`, observation normalization, action scaling, delayed actor updates) and turns on own-method linked preference sampling with online intervention duplication into the demo buffer plus moderate demo oversampling so human corrections are reused more aggressively while still training from scratch.
- `scripts/run_safetycar_minimal_own_gamepad_dense_best_local.ps1`: Windows/PowerShell counterpart to the rebuilt dense own-method launcher. It assumes a native Windows Safety-Gym environment, defaults to `gamepad_mode=local`, preserves the same linked-preference and intervention hyperparameters as the checked Linux launcher, and lets the user run the recommended dense human-teaching setup without manually retyping the full argument bundle.
- `scripts/run_safetycar_minimal_throttle_turn_dense_local.sh`, `scripts/run_safetycar_minimal_throttle_turn_safe_local.sh`, `scripts/run_safetycar_minimal_cardinal_dense_local.sh`: rebuilt-Safety-Gym launchers for comparing smoother or more discrete SafetyCar control surfaces against raw wheels. `throttle_turn` keeps a 2D continuous `[throttle, turn]` interface while preserving the same max wheel authority as the raw-wheel baseline, the safety-shaped variant adds the current best simple cost shaping, and `cardinal` keeps the same 2D interface but snaps execution to four motion primitives for a fair same-dimensional comparison against the other two modes.
- `scripts/run_safetycar_minimal_own_dataset_debug_local.sh`: rebuilt-Safety-Gym fixed-dataset own-method debug launcher. It preloads a specified Safety-Gym intervention dataset into replay, keeps the own-method linked-preference path active with configurable pref ratios/lambda settings, starts learning immediately from that same data source, and is intended for repeatable early-critic-debug runs where manual interventions should not have to be recollected every time.
- `scripts/run_safetycar_minimal_stage2_safety_purecost_local.sh`: focused rebuilt-Safety-Gym stage-2 safety launcher for overnight queue work. It bootstraps from the current safer dense checkpoint, keeps the actor LR low, disables auxiliary forward/backward/heading shaping by default, and applies only a collision-cost curriculum so safety fine-tuning can be evaluated without confounding directness bonuses.
- `scripts/run_safetycar_minimal_stage2_safety_clearance_local.sh`: focused rebuilt-Safety-Gym stage-2 safety launcher that adds a keepout-based clearance penalty on top of the safer dense checkpoint. It keeps actor LR low, leaves the auxiliary directness terms off, and is intended for overnight queue tests where collision-only shaping proved too sparse to improve obstacle avoidance.
- `scripts/run_safetycar_minimal_stage2_goalbase_purecost_local.sh`, `scripts/run_safetycar_minimal_stage2_goalbase_grounded_local.sh`, `scripts/run_safetycar_minimal_stage2_goalbase_clearance_local.sh`: autonomous stage-2 launchers that fine-tune the strong goal-reaching checkpoint rather than the already safety-shaped checkpoint. They are used for teacher-creation research and differ only in the shaping family: pure collision cost, directness-plus-cost, or clearance-plus-cost. The clearance branch is the current dense-safety candidate because it supplies obstacle-avoidance signal before actual contacts.
- `train_fast_sac_safetygym.py`, `train_fastsac_pvp_safetygym.py`, `train_fastsac_hilserl_safetygym.py`: Safety-Gymnasium entrypoints expose the manip-style preference/Lagrangian CLI flags so launches stay aligned across variants, even when the active training variant does not consume preference pairs.
- `ogbench/ogbench/wrappers/vec_env_wrapper.py`: forwards teacher/action-delta diagnostics into `extras['log']`, including aggregate deltas and component-wise intervention diagnostics (`/Teacher/diag_delta_xyz_l2`, `/Teacher/diag_delta_yaw_abs`, `/Teacher/diag_delta_gripper_abs`, threshold values/scales, divergence flags). When rendering OGBench manipulation envs in `human` mode through the vector wrapper, it now also tints UR5e/gripper materials in the passive MuJoCo viewer from the latest `teacher_intervened` / `teacher_reason` diagnostics by default (full takeover: arm+gripper red, gripper-only: gripper amber).
- `ogbench/ogbench/wrappers/vec_env_wrapper.py`: emits cube subgoal/progress episode metrics (`cubes_solved`, `cubes_total`, `cubes_solved_fraction`, `cube_max_target_error`) into `extras['log']` for W&B panels.
- `ogbench_utils/fastsac_ogbench_maze_env.py`, `ogbench_utils/fastsac_ogbench_manip_env.py`: apply explicit family-specific vector action clipping (`maze`: L2 clip `1.0`, `manip`: no global L2 clip) to avoid distorting manipulation action semantics.
- `ogbench_utils/fastsac_ogbench_maze_train.py`, `ogbench_utils/fastsac_ogbench_manip_train.py`: FastSAC OGBench is state-only (`obs_mode='state'` enforced); pixel observation handling was removed from this pipeline (use DRQ-v2 entrypoints for pixel training). Manip training can auto-load the latest matching reusable human-demo dataset into either the demo buffer or replay buffer before learning starts, and the manip path now has a repo-local PVP branch that keeps the same wrappers/checkpointing/W&B surface while switching the updater and buffer routing to novice-vs-human proxy value propagation.
- `ogbench_utils/buffers.py`: counterfactual replay buffer is removed; FastSAC uses replay/demo buffers plus optional preference pair buffer.
- `ogbench_utils/buffers.py`: `PreferenceTDBuffer` is removed as deprecated/unused; only `PreferencePairBuffer` remains for preference supervision.
- `ogbench_utils/update.py`: FastSAC updater no longer applies pixel random-shift augmentation or counterfactual critic penalties. Preference ranking loss now supports either `all` critic heads or the min-Q teacher/student pair only via `--pref_critic_scope`, and supports `margin` (softplus margin), plain `hinge`, `bradley_terry`, or `lagrangian` loss variants; alpha updates can optionally ignore intervened rows via `--alpha_update_student_only`, and the logger reports the resulting alpha-row usage and skipped alpha steps. The same updater now also hosts the manip-side EIL branch: deterministic actor updates, reward-free TD targets, per-row `eil_good` / `eil_bad` replay labels, and hinge-style threshold plus teacher-over-student pair losses that maximize Q for good actions and suppress Q for bad/pre-takeover learner actions without adding a separate trainer stack.

## Build, Test, and Development Commands
- To run/test anything locally, ALWAYS activate the local virtual env first: `conda activate fasttd3`
- See `README.md`
- Starting jobs on the cluster with automatic account detection works like this but is reserved for the human Use this info to create appropriate sbatch files and submit them: `bash submit_job.sh [experiment_name].sbatch`
- Logs: tail `logs/<name>_<JOBID>.log` for artifacts.
- If possible, ALWAYS run local smoke tests before telling me something is working and done.
- Always at least attempt a local smoke test before cluster submission to avoid queue/time waste.
- Assume local testing is CPU-only: validate startup/initialization and short-step execution, but keep smoke tests lightweight (no computationally intensive long training loops).

## Coding Style & Naming Conventions
- Python: PEP 8, 4 spaces, max line length 120.
- Names: modules/functions `snake_case`, classes `CamelCase`, constants `UPPER_SNAKE_CASE`.
- Scripts: executable with shebang; keep flags explicit and self-documenting.

## Testing Guidelines
- Framework: no unit test suite at root; add minimal smoke tests when changing training/eval.
- Quick check: run a short job and verify logs and model saved.

## Commit & Pull Request Guidelines
- Commits: imperative, concise, scoped (e.g., “Adjust SLURM script for booster queue”).
- Include why + scope when touching `run_experiment.sbatch`/`submit_job.sh` and configs.
- PRs: clear description, reproducible command(s), affected configs/paths, and sample logs/metrics (screenshots or snippets).
- Link related issues; note breaking changes and migration steps.

## Security & Configuration Tips
- WANDB runs offline by default as compute node on cluster does not have internet access; set `WANDB_MODE=run` to sync online.
- SLURM account replacement uses `.slurm_account`; review before submitting.
- Keep large artifacts out of Git; rely on `logs/`, `models/`, and external storage.

## Cluster Access Notes
- The cluster project workspace path is `/p/project1/hai_1074/meyjohann1/thesis` and mirrors the local repo structure (`logs/`, `models/`, `wandb/`, `run_*.sbatch`, `train_*.py`).
- For repeated non-interactive cluster access after one MFA login, use SSH connection multiplexing (`ControlMaster`/`ControlPersist`) with a control socket under `~/.ssh/cm/`.
- Prefer `scripts/cluster_readonly.sh` for cluster read operations (logs/wandb inspection). It enforces a read-only command allowlist and should be the default path for non-mutating cluster triage.
- The local experiment queue runtime lives under `experiment_queue/` in the repo root and is intentionally gitignored. It is an unsandboxed execution path for trusted launcher scripts only; prefer queueing existing repo scripts through the MCP server over direct long-running shell commands when autonomous experimentation is desired.
- Typical flow:
  - Start master once: `ssh -MNf juwels-booster.fz-juelich.de` (requires key passphrase + TOTP once).
  - Verify reuse: `ssh -O check juwels-booster.fz-juelich.de`.
  - Reuse for commands/log triage without re-prompt while master is alive: `ssh juwels-booster.fz-juelich.de 'cd /p/project1/hai_1074/meyjohann1/thesis && ...'`.
  - Close when done: `ssh -O exit juwels-booster.fz-juelich.de`.
- Cluster submission workflow (non-interactive):
  - Keep local + cluster repos in sync first (use local commit/push, then `git pullall` on cluster repo).
  - Use `bash submit_job.sh -y <script>.sbatch` to accept defaults without interactive prompts.
  - `-y` requires SLURM account auto-detection to work (or `SLURM_ACCOUNT`/`.slurm_account` to be set), otherwise it exits with a clear error.
  - After submit, verify queued/running jobs with `squeue -u "$USER"` or `squeue -j <JOBID>`.
- For log access, prefer direct paths in the cluster project dir, e.g.:
  - `/p/project1/hai_1074/meyjohann1/thesis/logs/drqv2_*.out`
  - `/p/project1/hai_1074/meyjohann1/thesis/logs/<run_name>_<JOBID>.log`

## W&B Debugging Workflow
- Canonical term is `wandb` (voice-to-text may transcribe this as `1B`; interpret `1B` as `wandb`).
- Cluster pipeline expectation:
  - Training jobs write offline runs to local `wandb/` on cluster (compute nodes have no internet).
  - `submit_job.sh` launches background sync on login node (`WANDB_MODE=online wandb sync --sync-all`) to upload those offline runs.
- Standard triage order:
  1. Inspect Slurm and per-run logs first (`logs/*.out`, `logs/*.err`, `logs/<run>_<JOBID>.log`).
  2. Inspect offline `wandb/` folders for the same job IDs (`offline-run-*` presence/timestamps).
  3. Check online W&B project/runs for corresponding uploaded runs.
- Interpretation:
  - No offline `wandb` run folder usually means failure happened before/during wandb initialization.
  - Offline folder exists but no online run usually means post-run sync failure (upload stage), not training-stage logging failure.
