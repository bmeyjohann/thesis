# SafetyGym Keyboard Mapping Correction

- For Safety-Gym intervention controls, use:
  - `W/S` or `Up/Down` for forward/backward motion.
  - `A/D` or `Left/Right` for turning left/right.
- This replaces the previous unintuitive rotated mapping.
