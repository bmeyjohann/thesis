# Direct Command Preference (Eval)

- If the user asks for immediate interactive testing setup, prefer:
  - direct `python ...` command(s)
  - minimal in-place script updates
- Avoid adding new helper launcher scripts unless the user explicitly requests one.
